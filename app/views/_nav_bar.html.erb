<nav class="nav">
<ul>
  <li> <%= link_to "home", root_path  %> </li>
  <li> <%= link_to "browse items", items_path  %> </li>
  <li> <%= link_to "browse merchants", merchants_path  %> </li>

  <% if current_user && current_user.role == "admin"  %>
    <li> <%= link_to 'Users', admin_users_path %> </li>
  <% else current_user && current_user.role == "admin" %>
    <li> <%= link_to "shopping cart", cart_path  %> (<%= @cart.items.count %>)</li>
  <% end %>

  <% if power_users %>
    <%= link_to 'Profile', profile_path  %>
    <%= link_to 'Orders', profile_orders_path %>
    <%= "Logged in as #{current_user.name}" %>
  <% end %>

  <% if current_user && current_user.role == 'merchant' %>
    <%= link_to 'Dashboard', dashboard_path %>
  <% end %>
  

  <% if current_user %>
    <li> <%= link_to "logout", logout_path, method: :delete %> </li>
  <% else %>
    <li> <%= link_to "login", login_path %> </li>
    <li> <%= link_to "Sign Up", register_path  %> </li>
  <% end %>
</ul>

</nav>
